<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelapp_part1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_bus"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_bus"
        android:supportsRtl="false"
        android:theme="@style/Theme.TravelApp_part1">
        <!--
            The API key for Google Maps-based APIs is defined as a string resource.
            (See the file "res/values/google_maps_api.xml").
            Note that the API key is linked to the encryption key used to sign the APK.
            You need a different API key for each encryption key, including the release key that is used to
            sign the APK for publishing.
            You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCBNh17AJ5m8vloX1XSvzguuMW7a1VtF3A" />

        <activity android:name=".ui.AddLocationActivity"/>
        <activity android:name=".ui.AddTravelActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>